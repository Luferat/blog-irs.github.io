// Define o título desta rota.
changeTitle('Políticas de Privacidade');

// Atualiza endereço do domínio.
$('.domainURL').html(location.origin);

// Atualiza endereço de e-mail.
$('.emailAddress').html(site.adminMail);